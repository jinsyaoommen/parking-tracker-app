import styled from 'styled-components';

import { colors } from './Theme';

export default styled.div`
  font-size: 14px;
  font-weight: 400;
  padding-top: 5px;
  margin-bottom: 0px;
  color: ${colors.valencia.light};
`;
